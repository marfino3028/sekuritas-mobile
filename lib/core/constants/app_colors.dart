import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand primary — deep teal
  static const primary = Color(0xFF0D9488);
  static const primaryDark = Color(0xFF0F766E);
  static const primaryLight = Color(0xFF2DD4BF);

  // Accent — sky blue (used in gradients)
  static const accent = Color(0xFF0EA5E9);
  static const accentLight = Color(0xFF38BDF8);

  // Gold accent for premium elements
  static const gold = Color(0xFFF59E0B);
  static const goldLight = Color(0xFFFCD34D);

  // Secondary green
  static const secondary = Color(0xFF10B981);

  // Backgrounds
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF8FAFC);
  static const surfaceVariant = Color(0xFFEEF2F7);

  // Dark backgrounds (header/card)
  static const darkBg = Color(0xFF0F172A);
  static const darkBg2 = Color(0xFF1A2744);
  static const darkSurface = Color(0xFF1E293B);

  // Status
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);

  // Text
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textHint = Color(0xFF94A3B8);

  // UI elements
  static const divider = Color(0xFFE2E8F0);
  static const cardShadow = Color(0x0D000000);
  static const overlay = Color(0x80000000);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);

  // Badge colors
  static const badgeWarning = Color(0xFFFEF3C7);
  static const badgeWarningText = Color(0xFF92400E);
  static const badgeError = Color(0xFFFEE2E2);
  static const badgeErrorText = Color(0xFFDC2626);
  static const badgeSuccess = Color(0xFFD1FAE5);
  static const badgeSuccessText = Color(0xFF065F46);
  static const badgeInfo = Color(0xFFE0F2FE);
  static const badgeInfoText = Color(0xFF0369A1);

  // Gradient stops
  static const gradientStart = Color(0xFF0D9488);
  static const gradientMid = Color(0xFF134E4A);
  static const gradientEnd = Color(0xFF0C4A6E);

  // Bottom nav
  static const navUnselected = Color(0xFF94A3B8);
  static const navSelected = Color(0xFF0D9488);

  // Glassmorphism
  static const glassWhite = Color(0x1AFFFFFF);
  static const glassBorder = Color(0x1AFFFFFF);
}
